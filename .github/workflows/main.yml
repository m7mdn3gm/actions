on:
  push:
    brance: 
      - master


jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3

      - name: "login to dockerhub"
        run: docker login --username os2022 --password dckr_pat_lNsBXc2u3nCAWRil3vIQf2rH_yA
          
